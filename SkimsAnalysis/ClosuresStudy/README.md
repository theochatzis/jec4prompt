# Closures study

Codes for producting closure plots of JECs to check their derivation was successful.

## `DrawClosures.C`
Script that makes the closure plots. Uses the `constants.json` in base directory of the repository for consistency.

